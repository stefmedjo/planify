<nav class="navbar navbar-expand-lg navbar-dark navbar-red">
  <div class="container">
    <a class="navbar-brand" href="{{ path("home") }}">{{ app_name }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      {% if app.user == null %}
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a href="{{ path("login") }}" class="nav-link">Login</a>
        </li>
        <li class="nav-item">
          <a href="{{ path("about") }}" class="nav-link">About</a>
        </li>
      </ul>
      {% else %}
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a href="{{ path("home") }}" class="nav-link">Dashboard</a>
          </li>
          <li class="nav-item">
            <a href="{{ path("project_list") }}" class="nav-link">Projects</a>
          </li>
          <li class="nav-item">
            <a href="#" class="nav-link">Team</a>
          </li>
          <li class="nav-item">
            <a href="{{ path("logout") }}" class="nav-link">Log out</a>
          </li>
        </ul>
      {% endif %}
    </div>
  </div>
</nav>